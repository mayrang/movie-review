"use client";
import { useState } from "react";
import SearchMovie from "./SearchMovie";

export default function Craate() {
  const [reviewValues, setReviewValues] = useState({});

  return <SearchMovie />;
}
